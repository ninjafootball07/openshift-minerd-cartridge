#!/bin/bash -eu

case "$1" in
    -v|--version)
        version="$2"
esac

mkdir -p ${OPENSHIFT_CPUMINER_DIR}/template
mkdir -p ${OPENSHIFT_CPUMINER_DIR}/logs

cp -r ${OPENSHIFT_CPUMINER_DIR}versions/${version}/cpuminer ${OPENSHIFT_CPUMINER_DIR}cpuminer
