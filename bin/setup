#!/bin/bash -eu

case "$1" in
    -v|--version)
        version="$2"
esac

mkdir -p ${OPENSHIFT_MINERD_DIR}/template
mkdir -p ${OPENSHIFT_MINERD_DIR}/logs

cp -r ${OPENSHIFT_MINERD_DIR}/versions/${version}/minerd ${OPENSHIFT_MINERD_DIR}/minerd
